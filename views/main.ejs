<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat - Messenger</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/main.css">
</head>
<body>
    <div class="container-fluid h-100">
        <div class="row h-100">
            <!-- SIDEBAR -->
            <div class="col-md-3 col-lg-3 sidebar">
                <!-- User Profile -->
                <div class="user-profile p-3 border-bottom">
                    <div class="d-flex align-items-center gap-2">
                        <img src="<%= urlAvatar %>" alt="User" class="rounded-circle" width="40" height="40" style="border-radius: 50%; border: 3px solid #8bd576;">
                        <div>
                            <div class="fw-bold"><%= username %></div>
                            <small class="text-muted online">Online</small>
                        </div>
                    </div>
                </div>
                <!-- danh sách user -->
                <div class="user-list p-3 border-bottom">
                    <button id="new-group-btn" class="btn btn-success mb-3" data-bs-toggle="modal" data-bs-target="#createGroupModal">+ Tạo nhóm</button>
                </div>
                <!-- Search Bar -->
                <div class="p-3">
                    <input type="text" id="searchInput" class="form-control form-control-sm" placeholder="Tìm kiếm...">
                </div>

                <!-- Conversation List -->
                <div class="conversation-list overflow-auto" id="conversation-list">
                </div>
                <!-- render danh sách user,conversation,group ở đây -->
                <!-- <div id="list" class="list" style="display:block;"></div> -->
                <!-- bottom sidebar -->
                <div class="bottom-bar">
                    <div class="icon-container-user" id="user-list-btn">
                        <i class="fa fa-user" id="user-icon"></i>  <!-- Icon người -->
                    </div>
                    <div class="icon-container-user" id="group-list-btn">
                        <i class="fa fa-users" id="group-icon"></i>  <!-- Icon nhóm -->
                    </div>
                </div>
            </div>

            <!-- CHAT AREA -->
            <div class="col-md-9 col-lg-9 d-flex flex-column h-100">
                <div class="h-100 d-flex flex-column">
                    <!-- Chat Header -->
                    <div class="chat-header bg-white border-bottom p-3 d-flex justify-content-between align-items-center">
                        <div class="d-flex align-items-center gap-3">
                            <img src="/image/default-avatar.jpg" alt="Avatar" class="rounded-circle" width="45" height="45">
                            <div>
                                <div class="fw-bold"></div>
                                <small class="text-success status-partner"></small>
                            </div>
                        </div>
                        <div class="d-flex gap-2" class="icons-container">
                            <div class="icon" id="phoneBtn">
                                <i class="fa fa-phone"></i> <!-- Biểu tượng gọi điện -->
                            </div>
                            <div class="icon" id="inforBtn">
                                <i class="fa fa-info-circle"></i> <!-- Biểu tượng thông tin -->
                            </div>
                        </div>
                    </div>
                    <!-- message và input -->
                    <div class="chat-panel d-flex flex-column h-100">
                        <!-- Messages Area -->
                        <div class="messages-area flex-grow-1 overflow-auto p-4" id="messagesArea">
                            <!-- Messages will appear here -->
                             <div class="text-center text-muted py-5">
                                <i class="fa fa-comment-dots fa-3x mb-3"></i>
                                <p>Chọn cuộc trò chuyện ở bên trái</p>
                            </div>
                        </div>
                        <!-- Input Area -->
                        <div class="input-area bg-white border-top p-3">
                            <div class="input-wrapper d-flex gap-2">
                                <input type="file" id="imageInput" accept="image/*" style="display: none;">
                                <button class="btn btn-light" title="Đính kèm" id="btnSendImage">
                                    <i>📷</i>
                                </button>
                                <textarea 
                                    id="messageInput" 
                                    class="form-control" 
                                    placeholder="Nhập tin nhắn..."
                                    rows="1"
                                ></textarea>
                                <button class="btn btn-primary" onclick="sendMessage()">
                                    Gửi
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
    </div>
</div>

<!-- Modal Tạo Nhóm -->
<div class="modal fade" id="createGroupModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-lg">
    <div class="modal-content">

      <div class="modal-header">
        <h5 class="modal-title">Tạo nhóm chat mới</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Đóng"></button>
      </div>

      <form id="createGroupForm">
        <div class="modal-body">

          <!-- Tên nhóm -->
          <div class="mb-3">
            <label class="form-label">Tên nhóm</label>
            <input
              type="text"
              class="form-control"
              id="groupTitleInput"
              placeholder="Nhập tên nhóm..."
              required
            />
          </div>

          <!-- Danh sách thành viên -->
          <div class="mb-2">
            <label class="form-label">Chọn thành viên</label>
          </div>

          <div id="groupUsersList" class="border rounded p-2 group-users-list">
            <div class="text-muted small">Đang tải danh sách người dùng...</div>
          </div>

        </div>

        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
            Hủy
          </button>
          <button type="submit" class="btn btn-primary">
            Tạo nhóm
          </button>
        </div>
      </form>

    </div>
  </div>
</div>

    <script>
        window.currentUserId = "<%= userId %>";
    </script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="js/main.js"></script>
    
</body>
</html>
